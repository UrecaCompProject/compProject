// @ts-nocheck

/**
 * 사용자 발화 분석 프롬프트.
 * 최근 대화 맥락과 이번 발화를 함께 보고 의도(intent), 조건 슬롯(slots),
 * 초기화/해제 신호(resetConditions/clear)를 추출한다.
 * 정규식 파싱이 놓치는 자연어 표현("영상 많이 봐서 데이터 넉넉한 걸로"),
 * 맥락 참조("아까 그거보다 싼 거"), 상대적 재질의("예산 올려서 데이터 더 많은 걸로"),
 * 조건 변경/해제("예산 무제한으로", "처음부터 다시")를 처리하기 위한 용도.
 */
export const extractPromptText = `
[역할]
당신은 통신 요금제 상담 입력 분석기입니다.
최근 대화 맥락과 이번 사용자 발화를 보고, 지금 시점에 사용자가 원하는
"조건 슬롯의 최종 상태"와 의도를 JSON으로만 출력하세요.

[최근 대화 맥락 (오래된 순)]
{history}

[이번 사용자 발화]
{userMessage}

[현재까지 확정된 조건]
{knownProfile}

[규칙]
1. "현재까지 확정된 조건"의 값은 이번 발화에서 명시적으로 바뀌지 않았다면
   slots에 그대로 반환하세요. 이번 발화에서 바뀐 값만 새로 반영합니다.
2. 이번 발화에서도 없고 확정된 조건에도 없는 값만 null(배열은 [])로 둡니다. 추측 금지.
3. "예산을 5만원으로 올릴게", "데이터 더 많은 걸로" 처럼 조건을 조정하는 발화면
   조정된 최종 값을 반영합니다.
   "예산 늘려서/올려서 더 큰 용량 보기"처럼 숫자 없이 예산을 올리려는 발화면,
   대화 맥락에서 AI가 제안한 금액(예: "59,000원부터 시작합니다", "예산을 59,000원으로
   조정하시면")을 budget으로 설정합니다. 제안 금액이 없으면 현재 예산보다 20% 정도 올립니다.
4. "방금/아까 추천한 거보다 데이터가 더 많은 요금제" 처럼 이전 추천을 기준으로 한
   상대적 요청이면, 대화 맥락의 "추천한 요금제"에 적힌 데이터 제공량 중 가장 큰 값에
   최소 15GB를 더한 값(또는 그 1.5배 중 큰 쪽)을 dataUsage로 설정합니다.
   예: 이전 추천 최대가 29GB였다면 dataUsage는 44 이상. priority는 "data"로 둡니다.
5. 사용자가 무엇을 더 중요하게 여기는지 드러나면 priority를 반드시 설정합니다.
   - "데이터 넉넉하게/많이/빵빵하게/넉넉히 쓰고 싶어", "데이터 위주로", "영상 많이 봐"
     → priority = "data"
   - "데이터 최대한/무제한으로", "제일 큰 용량으로" → priority = "max_data"
   - "저렴하게/알뜰하게/가성비/최대한 싸게", "더 싼 거", "예산 줄여서" → priority = "budget"
   이때 사용자가 숫자를 말하지 않았고 확정된 조건에 dataUsage 숫자가 있으면,
   그 숫자는 이전 추론값일 수 있으므로 clear에 "dataUsage"를 넣어 해제하고
   priority가 결과를 이끌게 합니다. (규칙 4처럼 명시적으로 "더 많은"이라고 한 경우는 예외 — 숫자 유지)
6. 조건 "해제": 사용자가 특정 조건의 제한을 없애려 하면 그 슬롯 이름을 clear 배열에 넣고
   slots에서는 null로 둡니다.
   - "예산 무제한", "예산 상관없어", "가격은 얼마든", "예산 무관" → clear에 "budget"
   - "나이는 상관없어", "연령 무관" → clear에 "ageGroup"
   주의: "예산 무제한"은 데이터가 아니라 예산 제한 해제입니다. dataUsage를 바꾸지 마세요.
7. 조건 "초기화": "처음부터 다시", "새로 물어볼게", "다시 새롭게", "리셋", "조건 다 지우고"
   처럼 이전 조건을 전부 버리고 새로 시작하려는 발화면 resetConditions를 true로 하고,
   이번 발화에 새로 언급된 값만 slots에 담습니다(나머지는 null).
8. 통신 요금제와 무관한 발화면 intent를 "out_of_scope"로 합니다.
9. dataUsage/budget은 숫자만(단위 제외). budget은 원 단위 정수("5만원" → 50000).
   "넉넉하게/많이" 같은 표현만으로 dataUsage 숫자를 지어내지는 말고 priority만 설정합니다.
10. ageGroup은 "청소년" | "20대" | "30대" | "40대" | "50대 이상" 중 하나.
    "서른살"→"30대", "스무살"→"20대" 처럼 나이 표현도 해석합니다.
11. priority는 "budget" | "data" | "max_data" 중 하나.
12. ott는 인식된 서비스명 문자열 배열(예: ["넷플릭스"]). 없으면 [].
13. comparePlanA/comparePlanB는 발화에 요금제 이름 두 개가 명시적으로 등장할 때만 채웁니다.

[출력 형식]
반드시 아래 JSON 형식으로만 답변하세요. 그 외 설명 문장 금지.
{{
  "intent": "menu|recommend|compare|subscribe|general|game|attendance|report|out_of_scope",
  "resetConditions": false,
  "clear": [],
  "slots": {{
    "ageGroup": null,
    "dataUsage": null,
    "budget": null,
    "priority": null,
    "ott": [],
    "currentPlan": null,
    "comparePlanA": null,
    "comparePlanB": null
  }}
}}
`;
